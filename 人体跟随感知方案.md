# 室内人体跟随：感知方案与远程调试

更新日期：2026-09-14

## 当前进展

已建立 `ros2_ws/src/` 下的主动 ROS 包、统一 TargetState 消息、A/B 启动入口与独立 demo。方案 A 已接入真实 `/bodylist` 适配器并通过真人叉腰锁定、测距和 Foxglove 验收；方案 B 仍发布 NOT_READY。demo 的模拟位置显式标记 `is_simulated=true`。

方案 A 相关包已在 Jetson ROS 2 Humble 原生编译；ASTRA S 深度、真实骨架、叉腰锁定、目标状态、掩码、距离/偏角和 Marker 已实机验证。方案 B 的 CUDA 基础算子通过，但 YOLO、ByteTrack 和配准深度流程尚未实现。

代码目录、同步方式和运行命令见 [README](README.md)，接口约定见 [公共接口](docs/interfaces.md)，后续任务见 [开发计划](docs/roadmap.md)。

## 方案流程图

两张图分别展示原厂骨架路线与推荐的 YOLO 深度路线。图中分别标出已提供或已接入、待开发和待扩展验证的部分。下位机与车辆运动不在当前实施范围。

- [方案 A：Astra SDK 骨架路线](docs/diagrams/方案A_Astra骨架跟随.svg)
- [方案 B：YOLO11 + 跟踪 + 深度路线](docs/diagrams/方案B_YOLO深度跟随.svg)
- [两方案并排对比 PNG（5780×3800）](docs/diagrams/人体跟随两方案对比.png)

## 1. 当前目标与边界

目标是在室内使用 Orin Nano Super 8GB 和奥比中光深度相机，识别并持续跟踪指定的人，输出其距离、方位和跟踪状态，通过 Mac 上的 Foxglove 与 SSH 远程调试。

**当前阶段先不考虑下位机与底盘控制。** 先验证相机、人体检测、目标跟踪、深度测距和数据展示，不把感知验证与车辆运动耦合。

本文区分三种信息：源码中已确认的行为、推荐的设计、尚需实机验证的事项。方案 A 标注的帧数、距离和状态来自 Jetson 实测；方案 B 的性能数字仍不能视为实测结果。

## 2. 硬件与软件基线

| 项目 | 当前信息 |
|---|---|
| 计算平台 | Orin Nano Super 8GB；JetPack 6.2 / Ubuntu 22.04 / ROS 2 Humble 已核对 |
| 场景 | 用户已确认室内使用 |
| 相机 | USB 实测 ORBBEC ASTRA S（2bc5:0402）；与最初商品描述不一致 |
| 源码 | `JP6.2_wheeltec_ros2_src_20260903/`，来自商家 Humble 源码目录 |
| 推荐环境 | 与载板支持匹配的 JetPack 6.x、Ubuntu 22.04、ROS 2 Humble；源码目录标注 JP6.2 |
| 调试方式 | Mac 上 Foxglove 查看数据，SSH 管理 Jetson 程序 |

### 相机资料不能混用

| 项目 | Astra Pro Plus | 商品页中的 Gemini Pro |
|---|---|---|
| 测距方式 | 单目结构光 | 商家标注双目结构光 |
| 标称深度范围 | 0.6–8 m | 商家标注 0.25–2.5 m |
| 深度图 | 640×480 @ 30 FPS | 商家标注 1280×800 @ 30 FPS |
| 接口 | USB 2.0 | 商家标注 USB 3.0 |
| 使用环境 | 室内 | 商家宣称支持室外强光，未独立验证 |

Astra Pro Plus 的上述主要规格可与[奥比中光官方资料](https://www.orbbec.com/products/structured-light-camera/astra-series/)核对。最大标称测距不等于能在该距离稳定识别、锁定和跟随人体。当前室内场景可先评估 Astra Pro Plus，但驱动与 USB 标识需按实物确认。

## 3. 厂商源码中已有的模块

| 模块 | 已确认内容 | 与当前任务的关系 |
|---|---|---|
| `ros2_astra_camera-master/astra_camera` | OpenNI 相机驱动，附 Linux ARM64 库 | 优先核对的相机接入路径 |
| `OrbbecSDK_ROS2-main` | 另一套奥比中光 ROS 2 驱动 | 根据设备支持选择，不能同时占用同一相机 |
| `wheeltec_bodyreader/bodyreader` | Astra SDK 人体骨架、姿势锁定、跟随示例 | 可评估和参考，授权与兼容性待验证 |
| `ultralytics_ros2` | YOLO 检测与结果发布，附 `yolo11n.pt` 等权重 | 推荐的人体检测起点 |
| `simple_follower_ros2` | 其中 visualTracker 使用 HSV 颜色检测与深度 | 不是人体识别算法 |
| `turn_on_wheeltec_robot` | 相机启动、压缩图像发布及底盘接口等 | 当前只关注相机部分，底盘后续再讨论 |

初查共 114 个 package.xml，可解析且无重复包名。关键 Astra/OpenNI 动态库经 `file` 检查为 Linux AArch64 ELF；这只证明架构匹配，不能证明运行依赖、授权或相机兼容性已经满足。详细源码证据保存在 WORKLOG.md。

## 4. 现成的人体骨架跟随方案

### 算法链路

```text
Astra 相机
  → Astra SDK Body Tracking
  → 人体 ID、关节三维坐标、人体质心
  → 姿势识别与目标 ID 锁定
  → 目标质心位置
```

核心人体识别封装在 SDK 动态库中，当前代码包提供的是接口调用和应用逻辑，不能从这些 ROS 源码确定其内部网络或训练方法。

`bodydata_process.cpp` 使用肩、手、脊柱、脚等关节判断姿势；满足双手叉腰条件时会锁定该人体 ID。`follower.cpp` 实际使用被锁定人体的质心横向位置和前向距离，骨架主要服务于姿势交互和目标选择。

项目适配器采用更宽松、可配置的叉腰判定：手高于脊柱基点 20 mm、手与对应肩膀横向差小于 160 mm、肩高于手 20 mm，并以最近 10 帧中满足 3 帧作为锁定或切换条件。空间阈值比厂商原始 50/100/50 mm 更宽，短窗口投票用于抵抗关节抖动并避免孤立单帧误锁；五项参数均由正式 `route:=astra` 暴露。

SDK 的 `worldPosition` 不能直接当作房间地图坐标；坐标轴、单位和相机到机器人坐标的变换都应核对。

### 已发现的限制

- `main.cpp:254` 授权字符串仍为 `<INSERT LICENSE KEY HERE>`。SDK 在该设备上的实际授权状态待核实，不能直接宣称可用或不可用。
- 附带旧 SDK 与运行库，需要在 Jetson 的目标环境验证加载与稳定性。
- 跟随示例的目标距离固定为 2000 mm；控制参数只在启动时读取。
- 跟随节点没有独立的数据超时处理；上游另有未锁定时发布零速度的逻辑，但不能覆盖上游断流。车辆控制属于后续阶段。

参考：[Astra SDK](https://www.orbbec.com/developers/astra-sdk/)、[Body Tracking 接口](https://developer.orbbec.com.cn/technical_library/Astra_SDK2.1.3/html/group__cpp__body__api__ref.html)。

## 5. 推荐的 YOLO 加深度测距方案

骨架识别与深度测距是不同功能。识别回答“哪里有人、姿态如何”，跟踪回答“哪条轨迹属于选定目标”，深度回答“该区域距离相机多远”。

```text
RGB 图像 → YOLO 人体检测 → 跟踪器 → 选定目标
                                          ↓
深度图 → RGB/深度配准与时间匹配 → 人体区域深度筛选
                                          ↓
                         距离、三维位置、方位角、有效性
                                          ↓
                              ROS 话题 → Foxglove
```

### 深度从哪里来

Astra Pro Plus 使用红外结构光测距，深度计算由相机系统完成，驱动输出深度图。本方案直接使用相机深度，不额外采用单目神经网络估计距离，也不要求先做 SLAM。

### 建议的数据处理

1. 根据相机驱动能力做 RGB/深度空间配准，结合时间戳匹配图像。仅分辨率一致不足以保证同像素对应同一空间点。
2. 使用已锁定目标的躯干区域取深度，避免直接读取人体框中心的单个像素。
3. 根据消息编码与驱动约定统一单位，剔除零值、非有限值、超出有效范围的值，并检查取样区域边界。
4. 统计有效深度比例，使用中位数或深度聚类，结合上一帧目标位置拒绝异常跳变。多人重叠时，整个框的中位数可能落到另一个人或背景上，需要更严格的关联，必要时引入分割掩膜。
5. 用相机内参将代表像素位置和深度反投影，输出相机坐标下的位置；若需要机器人或地图坐标，再使用 TF 变换。
6. 深度不足、目标丢失或消息过期时输出无效状态，不把旧距离当作当前有效测量。

对于经过适当校正且深度对齐到对应图像的针孔模型，设代表像素为 `(u, v)`，光轴深度为 `Z`：

```text
X = (u - cx) * Z / fx
Y = (v - cy) * Z / fy
```

采用 ROS 相机 optical 坐标约定时，X 向右、Y 向下、Z 向前。前向深度 Z、水平距离 `sqrt(X² + Z²)` 和空间距离 `sqrt(X² + Y² + Z²)` 不同；话题字段必须说明定义。相机水平偏角可用 `atan2(X, Z)` 表示，转换到机器人坐标时再核对方向。

### 当前源码需要调整的地方

- `astra_pro.launch.xml` 默认 `depth_registration=false`、`color_depth_synchronization=false`；必须验证并配置数据对应关系。
- `visualTracker.py` 只对局部深度排除 NaN 后求均值，不能直接作为完善的人体测距模块。
- YOLO 默认启动文件使用交通标志模型和绝对路径，输入 `/image_raw`；Astra 当前启动链的彩色图像为 `/camera/color/image_raw`。
- YOLO 当前只调用 `predict()`，尚无指定目标跟踪、深度融合和目标状态管理。
- `detected_image` 发布时未复制输入 header，后续需保留时间戳和 frame_id。

## 6. YOLO11 是否够用

**推荐先用 YOLO11n 检测版，配合 ByteTrack 与相机深度。** 这是针对当前需求的工程起点，尚无本机实测性能数据。

| 模型 | 输出 | 选择条件 |
|---|---|---|
| `yolo11n.pt` | 人体框、类别、置信度 | 第一版普通人体跟踪；源码包已附带，尚未加载验证 |
| `yolo11s.pt` | 同类输出，模型更大 | n 版在目标场景漏检明显时做实测对比 |
| `yolo11n-pose.pt` | 人体框与二维人体关键点 | 需要举手选人、姿态展示等交互时评估 |
| YOLO11 分割版 | 人体掩膜 | 深度区域混入背景或相邻人体较多时评估 |

普通检测权重不输出骨架。Pose 版关键点是二维结果，三维关节还需结合有效深度；遮挡或低置信度关键点不能直接当作可靠空间测量。Pose 模型可同时提供人体框与关键点，不必默认同时运行一个检测模型和一个姿态模型。

初始部署可评估 640 输入尺寸、单路 RGB、TensorRT FP16。TensorRT 导出与加载需匹配 Jetson 环境，厂商当前检测代码还需要适配 engine 加载路径，不能直接假定换个文件后缀就可用。模型选择应看端到端延迟、漏检和 ID 切换，不只看独立推理 FPS。

ByteTrack 是起步方案，轨迹 ID 不是持久身份。多人交叉、相机移动、长遮挡后恢复需要实测，必要时评估带相机运动补偿及可选 ReID 的 BoT-SORT；ReID 也不能保证永不串人。

参考：[YOLO11 模型](https://docs.ultralytics.com/models/yolo11/)、[跟踪器](https://docs.ultralytics.com/modes/track/)、[TensorRT](https://docs.ultralytics.com/integrations/tensorrt/)。

## 7. Foxglove 与 SSH

Jetson 运行 ROS 节点和 `foxglove_bridge`；Mac 使用 SSH 修改代码、启动程序、查看日志，Foxglove 负责图像与数据展示。通过桥接连接时，Mac 不需要安装完整 ROS。

目标环境已安装 ROS 2 Humble 后，可在 Jetson 安装并启动：

```bash
sudo apt update
sudo apt install ros-humble-foxglove-bridge
source /opt/ros/humble/setup.bash
ros2 launch foxglove_bridge foxglove_bridge_launch.xml
```

当前小车的 Bridge 监听 Wi-Fi 接口。在同一路由器网络中，Foxglove 选择 Foxglove WebSocket，直接填写：

```bash
ws://192.168.1.240:8765
```

离开该 Wi-Fi 或不希望开放局域网端口时，可改用 SSH 隧道备用入口 `scripts/open_foxglove_tunnel.sh`。

### 第一阶段调试面板

| 面板 | 数据 | 车端要求 |
|---|---|---|
| Image | RGB、深度、检测结果图 | 驱动输出有效图像；检测结果图保留 header |
| 3D | 点云、目标位置、坐标系 | 正确的 frame_id、TF 和支持的可视化消息 |
| Plot | 距离、偏角、深度有效率、消息年龄 | 感知节点发布数值，说明单位与含义 |
| Raw Messages / 状态展示 | 目标 ID、是否锁定、是否丢失、位置是否有效 | 显式发布状态，不只写在终端日志里 |
| Parameters | 检测阈值等可调参数 | 声明参数、校验修改值并在运行逻辑中应用 |

面板通常通过 Foxglove 界面配置并保存布局，无需手写前端。但 Foxglove 无法直接读取程序内部变量，需要 ROS 数据接口。参数显示可修改也不意味着代码已经动态应用。

当前源码没有 Foxglove 桥接和布局配置。相机启动已有 JPEG 彩色图和 compressedDepth 重发布；需确认显示端对具体深度编码的支持。Wi-Fi 下优先查看压缩 RGB 和必要数值，点云按需启用。当前不配置 Teleop，也不接车辆运动。

参考：[ROS 2 连接](https://docs.foxglove.dev/docs/getting-started/frameworks/ros2?modality=recorded)、[Image](https://docs.foxglove.dev/docs/visualization/panels/image)、[3D](https://docs.foxglove.dev/docs/visualization/panels/3d)。

## 8. 感知阶段实施与验收顺序

1. **相机验收：** 确认实物型号、USB 标识、JetPack/ROS 版本，验证 RGB、深度、内参、时间戳和配准。检查不同距离和光照下的有效深度。
2. **检测验收：** 加载人体检测模型，显示带框图像；覆盖正面、背面、侧面、坐下、近距离部分出画等情况。
3. **目标跟踪：** 初期用明确的目标选择接口锁定目标 ID；接口形式可为服务或后续自定义交互，不能假定 Foxglove Image 已有现成选人按钮。
4. **深度定位：** 发布距离、方位、有效性、深度有效率和消息年龄；对照已知距离检查偏差与抖动。
5. **连续性验证：** 测试第二人进入、两人交叉、遮挡、重新出现和图像断流，记录 ID 切换、丢失和恢复行为。目标丢失时不自动改跟最近的人。
6. **性能验证：** 记录实际图像频率、处理频率、数据年龄、延迟、GPU/内存占用和温度。再决定是否需要 TensorRT、s 版、Pose 或 ReID。

本阶段交付应是一个能在 Foxglove 稳定展示目标与位置的感知程序。底盘通信、速度控制、接管和车辆停车验证留到后续阶段。

## 9. 待确认事项

- 相机实际型号和 USB 标识。
- Jetson 是否已到手、系统版本、ROS 安装状态和 SSH 地址。
- 相机配准方式、深度编码与单位、移动时 RGB/深度时间误差。
- 首版是否需要姿势交互；目前建议先做检测与跟踪。
- 旧 Astra Body Tracking 的授权状态与运行兼容性，仅在评估该路线时处理。

## 10. 骨架、跟随与卡尔曼滤波的关系

### 当前源码的实际链路

1. `main.cpp` 从 Astra SDK 取得人体列表、人体 ID、关节和质心，并发布 `/bodylist`。SDK 内部算法封装在二进制库中，无法从当前应用源码确认其是否使用卡尔曼滤波。
2. `bodydata_process.cpp` 用关节位置判断姿势，叉腰时记录 `lock_body_id`；后续按 ID 取出目标质心，发布 `/body_posture`。
3. `display.py:114` 起还承担简单的目标恢复功能：锁定时记录躯干区域平均 RGB，失去锁定而仍有人时，对候选人体的平均 RGB 做阈值比较，匹配后发布 `/recoveryid`，由处理节点更新锁定 ID。这不是神经网络 ReID，光照和同色衣服可能导致误匹配。
4. `follower.cpp` 使用质心 Z 作为距离，用 X/Z 作为横向偏差量。X/Z 是角度的正切形式，不是严格的弧度角。
5. 控制采用比例项加相邻误差差分项，属于简化 PD；没有积分项，差分也没有除以实际 dt。另有距离 ±80 mm、横向比值 ±0.01 的死区，以及速度限幅。死区、限幅和 PD 均不等于卡尔曼滤波。

该人体应用链的可见源码没有显式的卡尔曼状态预测/协方差更新实现。厂商整车启动链另外包含 `robot_localization/ekf_node`，用于机器人自身状态估计；不能据此说人体质心已经过 EKF 滤波。

### 推荐路线里的两个位置

- **图像跟踪层：** Ultralytics ByteTrack 内置卡尔曼滤波，状态描述检测框中心、宽高比、高度及其变化率，用于预测下一帧检测框并辅助匹配维持轨迹。YOLO 的 `predict()` 只是检测推理，当前厂商检测节点尚未接入 ByteTrack。
- **空间定位层：** 可另加位置滤波，使用深度测得的 X/Z 和实际时间间隔估计位置及速度，例如状态 `[X, Z, Vx, Vz]`。这是可选设计，尚未实现。第一版可先用稳健深度统计加简单平滑，测出抖动和延迟后再决定是否需要卡尔曼。

检测框卡尔曼不自动对深度做滤波，也不提供持久身份保证。卡尔曼通过“运动预测 + 新测量校正”抑制噪声并辅助短时跟踪；预测不能当作当前实测有效值，长时间遮挡仍需丢失状态和重确认。相机随车运动时，相机坐标中的位置变化包含自运动，需要坐标补偿或调整运动模型，不能把估计速度直接解释为人的世界速度。

参考：[ByteTrack 实现](https://docs.ultralytics.com/reference/trackers/byte_tracker)、[卡尔曼实现](https://docs.ultralytics.com/reference/trackers/utils/kalman_filter)。

## 11. 相关项目文件

- `AGENTS.md`：协作约定与当前任务边界。
- `WORKLOG.md`：源码初查证据和变更记录。
- `淘宝信息/`：用户保存的商品页和产品图片。
- `JP6.2_wheeltec_ros2_src_20260903/`：厂商源码，本次文档整理未修改其中内容。

## 串口代码暂存（2026-09-14）

已按用户要求把厂商底盘串口驱动和 serial、wheeltec_robot_msg 依赖复制到 `ros2_ws/src/chassis_vendor/`。当前 COLCON_IGNORE 跳过构建，感知启动不加载底盘，不输出车辆指令，尚未编译或连接下位机。已有协议和后续启用工作见 [串口说明](ros2_ws/src/chassis_vendor/README.md)。

## 方案 A 最新联调（2026-09-14）

ASTRA S 深度、真实人体骨架、叉腰锁定、质心测距、掩码、Marker 和 Foxglove 已实机跑通。正式 `route:=astra` 现启动 bodylist_adapter；15 秒验收中约第 1.93 秒锁定 ID 41，随后 363 条状态均为有效 TRACKING。SDK 仍打印授权提示，但没有阻止本次输出，其长期含义待厂商解释。详细入口与限制见 [方案 A 联调记录](docs/方案A联调记录.md)。

根据首次手势命中率，默认叉腰阈值已从厂商等价的 50/100/50 mm 放宽为 20/160/20 mm，并改为 10 帧内 3 票。Jetson 编译、7 个逻辑测试和正式 route 合成状态机测试通过；在线进程需在小车网络恢复并重启后加载新默认值。
